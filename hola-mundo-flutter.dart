import 'package:flutter/material.dart';

void main() {
  runApp(
    Center(
      child: Container(
        color: Colors.amberAccent,
        child:  Text(
          'Hello, world!',
          textDirection: TextDirection.ltr,
        ),
      )
    ),
  );
}
